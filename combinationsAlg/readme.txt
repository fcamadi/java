

There are two kind of entities:

-elements

-sets (of elements)


The algorithm, given an input of elements and/or sets, must return the combinations of elements 
that contain all the elements in the input.

